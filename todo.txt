todo

-> check if the diagonal terms in the K matrix have an impact on the results (set to 0 instead of 1/2 factor)

-> give the results as a function of the bins

-> read Wang et al. 2025



19/05/2026: rewrote the class (IonFormation2) to compute correctly the alpha term correctly (considering the N_pos/neg*N_<dp_neg/pos)
	-> doesn't seem to work well looking at the plot of Q_snow neg and pos.

20/05/2026:
	- Matt's code doesd not seem to work with updated verions of Pandas and Numpy (it gives importante values for Q_snow oscillating around 0).
	- Fixed IonFormation2, so it shows good results for Q_snow. Now need to separate the values per bin.

	- Seem to have succeded calculating the weight of each terms per bin. I got rid of the smps data (which are outside of the studied window right)?
	- Need now to show the results with a smart figure.

21/05 : 
	- new day new issue. The coag_loss_function returns a DataFrame full of NaNs. The Reason is when summing the vectors for all background particle effect, there's 	apparently Always at least one NaN: the sum becomes NaN.
	- Fixed the issue by ignoring the NaN (could be a problem: NaN doesn't mean no particle, but no measuring. There could be an effect but we're considering no particle).
	- Better fixing: the to last columns of the psd are empty. Droping them from the beginning allows to still consider the NaNs in case the is one in the other columns. 	The line for ignoring the NaNs is commented.
	- The code now plot the Q_snow equation members showing the contribution for each bin

22/05:
	- set the git hub
	- changed the preprocess function so it creates de Data-clean/ folder
	- made the plot_member function to plot the contribution of each members for a range of size bin

25/05: Made meteo.py, that "detects" the BSE (averaging the true wind speed velocity over three days and selecting when wind speed > 12m.s-1)

26/05: 
	- Made global_conc.py, to study the concentration and wind over the time, for different range of size bins.
	- meteo.py now slice the psd when the positive ion concentration is above 2000 cm-3. I guess it's not the best.
	- I manually stored the npf events in a list in meteo.py

27/05:
	- exe_all.py plots the results for all npf events and store them in dedicatd folders
	- Noise treatment:	- the data are now resampled ('10min' like before) and then rolling.median with adaptable window (2h) (shall we do that in preprocess?)
						- update on the class: possibility to do wider difference when computing dNdP/dt 
						- No Savitzky-Golay filter yet
	- Preprocess creates ..._10min.parquet files that are already resampled and rollmeaned (should they be rollmeaned ? no, could rollmean in the class to have more control)

28/05:
	- had meeting. See what's needed below
	- Changed the smooth method. No smoothing is set by default
	- the conc wind and events plot is in the class (plot_events)
	- compute the ratio beetween pos and neg -> need to care about the divisions by zero.
	


what's needed : 
	- consider calculating the importance of the members by ratio (ie Q_snow / member term)
	- should maybe plot_member for all size bins to see if some are behaving differently
	- improve layout for hm_plot
	- calculate the growth rate:
		The growth rate can not be calculated for the whole time period, it needs to be calculated for each BSE. For the GR to be valid, the particle size needs to grow monotically.
	- should exe_all.py store results in csv?



Answers to first discussion:
	- study one summer event

	- do not rollmean the data before computing Q_snow
		-> done

	- ratio pos/neg
		-> done. Although, I cut of the low dividers values (<1), so we can see something on the graph. Should consider that 
	
	- met: when studying an event, compute and show mean temperature, wind and other useful quantities
		->

	- Consider T and P in CoagL
		-> done. The result is that the effect of CoagL decrease a bit, but it doesn't change a lot the interpretation of the results.

	- from which bin size is the production rate led by something else than dN/dt
		-> I changed plot_members so it plots for 4 different size-bin ranges:
		- Up to 0.87 nm, the production rate behave the same way
		- From 0.87 to ..., they behave a bit differently but are also clearly driven by the CoagL
		- From 10. (8.66) nm, the production rate seem to be clearly driven by dN/dt

		-> I plotted the concentration over the whole winter for each size bins.
			- The small aerosols (from 0.75 nm to 1.33/1.54 nm) behave differently than the others. Their concentraion peaks are out of the events.
			- The bigger aerosols (from 20.54 nm) Also behave a bit differently. They still have peaks during NFP, but show other peaks
			- same for neg and pos

		-> for sub nm particles, the negative ion production rate seems to be higher than the positive



Questions: 	- I sliced the NPF events manually. Papers state that it is when the snow drift density at 0.1 m above ground must exceed 10e-5 kg m-3, but we don't have the Data
				-> do we have the data ?
			- I sliced the NPF looking at the total concentration (all size bins).
				When looking at concentrations for each concentration, we see that the peaks happen at different times, often out of the chosen NPF
				How should I define the NPF events then ???
			- Are the data I have clean? There's some time stamp with very high values which compress the output plots. Are we expecting such a variability in the measurments?



Savitzky-Golay filter 

Size bins = [0.75,  0.87,   1.0,  1.15,  1.33,  1.54,  1.78,  2.05,  2.37,  2.74,
        3.16,  3.65,  4.22,  4.87,  5.62,  6.49,   7.5,  8.66,  10.0, 11.55,
       13.34,  15.4, 17.78, 20.54, 23.71, 27.38, 31.62]